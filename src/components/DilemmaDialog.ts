// Fully implemented in Step 4 — "One attempt" acceptance modal
export class DilemmaDialog {
  show(
    _stake: string,
    _score: number,
    _onAccept: () => void,
    _onDecline: () => void,
  ): void {}
}
